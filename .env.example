# Optional. If set, the Hugging Face API calls (both in the GitHub Action and
# in the runtime fallback) include it for higher rate limits. Public model
# metadata works fine without it.
#
# To use it on GitHub Actions, add a repository secret named HF_API_TOKEN.
# The workflow .github/workflows/sync-trending.yml reads it automatically.
HF_API_TOKEN=
